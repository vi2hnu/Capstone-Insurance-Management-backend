package org.example.providerservice.model.enums;

public enum NetworkType {
    IN_NETWORK, //cashless for users , the company pays the hospital
    OUT_NETWORK //the user pays the hospital and company pays the user
}
